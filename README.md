# 📝 Vibe Coding Todo App

This project is a modern Todo application built using **Bolt.new**, powered by **React + Vite** and **Supabase**.

## 🚀 Features
- Create, Read, Update, and Delete (CRUD) tasks
- Real-time database connection with Supabase
- Dockerized environment for easy deployment

---

## 🐳 How to Run with Docker

Follow the steps below to build and run this application using Docker.

### 1. Prerequisites
Since this application uses Supabase, you need to provide your API keys.

Create a **`.env`** file in the root directory of the project and add your Supabase credentials:

```env
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here  

---
**Author:** M.F. Kazım Uz | Student ID: 23051200003